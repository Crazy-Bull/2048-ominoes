# 4 spawn rate (default: 0.1)
P = 0.1
# name of temporary folder (default: 'tmp') (make sure that two processes do not use the same folder)
TEMP_FOLDER = 'tmp4'
# whether to use cProfile for timing (default: False)
USE_CPROFILE = False
# list of polynomino indices to process (default: list(range(0, 369)))
workflow = [88, 89, 198, 199]